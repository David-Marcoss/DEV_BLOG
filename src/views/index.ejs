<%- include ('partials/header')%>
<body>
    <%- include ('partials/navbarMenu')%>
    
    <div class="container" id="home-conteudo">
        <br>
        <div class="jumbotron" id="capa-home">
            <h1 class="display-4">Bem vindo ao DevBlog !</h1>
            <p class="lead">
                Nosso objetivo é proporcionar uma experiência enriquecedora, repleta de insights e conhecimentos
                que impulsionem seu desenvolvimento profissional e pessoal. Junte-se a nós nessa jornada de aprendizado
                e descoberta!</p>
            <hr class="my-4">
            
            <% if (!isAuthenticated) {%>
                <p>Registre-se em nossa plataforma e crie seus próprios artigos de forma fácil e rápida.</p>
                <p class="lead">
                <a class="btn btn-primary btn-lg" href="/users/login" role="button">Registre-se</a>
                </p>
            <% } else if (isAuthenticated && isAdmin) {%>
                <p>Seja bem vindo! Crie novos artigos de forma fácil e rápida.</p>
                <p class="lead">
                <a class="btn btn-primary btn-lg" href="/admin/articles/new" role="button">Criar Artigo</a>
                </p>
            <% } %>

        </div>
        <br>
        
        <h2> Artigos</h2>
        <hr>
        
        <% articles.forEach( article => {%>
            <div class="card">
                <div class="card-header">
                    <h3> <%=article.title%> </h3>
                    <p style="color: rgb(144, 143, 143);">Categoria: <%=article.category.name%>  | Autor: <%=article.user.name%></p>
                </div>

                <div class="card-body">
                    <p><%= article.resume %></p>
                    <a href="/articles/<%= article.slug %>"><button class="btn btn-success"> Ler </button></a>
                </div>
            </div>
            <br>
        <%})%>

        <%- include ('partials/pagination')%>
       
    </div>

    
</body>

<%- include ('partials/footer')%>